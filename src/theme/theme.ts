export type Colors = {
  primaryBackground: string;
  secondaryBackground: string;
  primary: string;
  primaryText: string;
  headerText: string;
  error: string;
};

export const colors: Colors = {
  primaryBackground: '#efece7',
  secondaryBackground: '#FFFFFF',
  primary: '#3b4257',
  primaryText: '#000000',
  headerText: '#FFFFFF',
  error: '#FF0000',
};
